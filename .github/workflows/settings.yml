---
name: Settings

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 10 * * *'

jobs:
  call-settings:
    uses: telekom-mms/.github/.github/workflows/github_repository.yml@2d0fe8ac430ca520fc6aad1b8628d4647f69c8b6 # main
    secrets:
      GH_APP_CREDENTIALS_TOKEN: ${{ secrets.GH_APP_CREDENTIALS_TOKEN }}
