---
name: Settings

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 10 * * *'

jobs:
  call-settings:
    uses: telekom-mms/.github/.github/workflows/github_repository.yml@d6a3d305c830c176814085a2c52340bfe4f2f99c # main
    secrets:
      GH_APP_CREDENTIALS_TOKEN: ${{ secrets.GH_APP_CREDENTIALS_TOKEN }}
